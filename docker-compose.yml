version: '3.8'

services:
  visa-planner:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: visa-planner-app
    ports:
      - "3000:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
